- name: Backend setup
  hosts: all
  become: yes
  tasks:
    - name: disable nodejs present version
      ansible.builtin.shell: dnf module disable nodejs -y
    - name: enable nodejs current version 
      ansible.builtin.shell: dnf module enable nodejs:20 -y
    - name: install nodejs
      ansible.builtin.dnf:
        name: nodejs
        state: present
    - name: remove directory
      ansible.builtin.file:
        path: /app
        state: absent
    - name: create directory
      ansible.builtin.file:
        path: /app
        state: directory
    - name: add app user
      ansible.builtin.user:
        name: sri
    - name: copy backend service
      ansible.builtin.copy:
        src: backend.service
        dest: /etc/systemd/system/backend.service
    - name: download backend code and unzip it
      ansible.builtin.unarchive:
        src: https://expense-artifacts.s3.amazonaws.com/backend.zip
        dest: /app
        remote_src: yes
    - name: download dependencies
      ansible.builtin.shell: npm install
      args: 
        chdir: /app
    - name: enable and restart backend service
      ansible.builtin.systemd_service:
        name: backend
        enabled: true
        state: restarted
        daemon-reload: true
    - name: install mysql setup
      ansible.builtin.dnf:
        name: 
          - mysql
          - python3-PyMySQL
        state: present
    - name: connecting to the datab
      community.mysql.mysql_db:
        state: import
        name: all
        target: /app/schema/backend.sql
        login_user: root
        login_password: Sriharsha@1
        login_host: mysql.sriharsha.shop 